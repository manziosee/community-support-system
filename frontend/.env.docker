# Docker Environment — runtime values (non-VITE vars only)
# VITE_* vars are baked at build time via Dockerfile ARG / docker-compose build.args
NODE_ENV=production

# Feature Flags (baked at build time — set via docker-compose build args if needed)
# VITE_ENABLE_2FA=true
# VITE_ENABLE_EMAIL_VERIFICATION=true
