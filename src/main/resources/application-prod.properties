# Production Profile - Neon PostgreSQL
spring.application.name=CommunitySupportSystem

# Neon PostgreSQL datasource
spring.datasource.url=${PROD_DB_URL:jdbc:postgresql://ep-sparkling-sky-a44gtbjp-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${PROD_DB_USERNAME:neondb_owner}
spring.datasource.password=${PROD_DB_PASSWORD:npg_0hEk5QoiUsKO}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Configuration for Production
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1200000

# JPA / Hibernate for Production
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.show_sql=false

# Production Logging (Reduced verbosity)
logging.level.om.community.supportsystem=INFO
logging.level.org.springframework.web=WARN
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN

# Server Configuration
server.port=${PORT:8080}

# Swagger/OpenAPI Configuration (Enabled for API documentation)
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# JWT Configuration
jwt.secret=${JWT_SECRET:5f566298b0ba4f8e38c1ff7c6e0844a1}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Email Configuration (Production)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:darkosee23@gmail.com}
spring.mail.password=${MAIL_PASSWORD:pciv ygjl uvwk petm}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email settings
spring.mail.test-connection=false
spring.mail.default-encoding=UTF-8

# CORS Configuration
cors.allowed.origins=http://localhost:3000,https://community-support-system.vercel.app

# Security Headers
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=strict

# Production Error Handling
server.error.whitelabel.enabled=false
server.error.include-stacktrace=never
server.error.include-message=never

# Actuator (Health Check)
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
management.health.db.enabled=true